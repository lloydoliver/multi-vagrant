unless Vagrant.has_plugin?('vagrant-lxc')
  puts 'ERROR: vagrant-lxc plugin is not installed!'
  exit 1
end

unless system('which lxc-create >/dev/null')
  puts 'ERROR: lxc not installed!'
  exit 1
end
