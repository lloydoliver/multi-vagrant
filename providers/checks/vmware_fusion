unless Vagrant.has_plugin?('vagrant-vmware-desktop')
  puts 'ERROR: vagrant-vmware-desktop plugin is not installed!'
  exit 1
end

unless system('which vmcli >/dev/null')
  puts 'ERROR: VMWare Desktop or Fusion is not installed!'
  exit 1
end
